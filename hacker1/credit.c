#include <stdio.h>
#include <cs50.h>
#include <string.h>

int main()
{
    printf("number:");
    long long number = 0;
    number = GetLongLong()
    int total = 0;
    for (int i = 0, n = strlen(number); i < n; i++)
    {
        printf("%c", number[i];
    }

}
